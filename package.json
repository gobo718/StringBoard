{
  "name": "stringboard-engine",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev --config worker/wrangler.toml",
    "predeploy": "npm install --ignore-scripts",
    "deploy": "wrangler deploy --config worker/wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
